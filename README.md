## Front

```
cd client
yarn install
yarn start
yarn build
```

## Server

Generate RSA key in `rsa/jwt.pem` and `rsa/jwt_pub.pem`

Edit sql info in `server/model/index.js` 

```
cd server
yarn start
```
